<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beautiful Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="main w-full max-w-md mx-auto bg-white rounded-lg shadow-lg p-6">
        <h1 class="text-3xl font-bold mb-6 text-gray-800">Create a New Post</h1>
        <form action="/create" method="post">
            <div class="mb-5">
                <label for="title" class="block text-gray-700 text-sm font-semibold mb-2">Title</label>
                <input name="title" type="text" id="title" placeholder="Enter your title"
                    class="border border-gray-300 rounded-lg p-3 w-full focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200">
            </div>
            <div class="mb-5">
                <label for="content" class="block text-gray-700 text-sm font-semibold mb-2">Content</label>
                <textarea name="details" id="content" placeholder="Write something..."
                    class="border border-gray-300 rounded-lg p-3 w-full h-40 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200"></textarea>
            </div>
            <div class="flex justify-end">
                <button type="submit"
                    class="bg-blue-500 text-white font-bold py-2 px-6 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200">
                    Submit
                </button>
            </div>
        </form>

        <div class="task mt-6 bg-gray-50 p-4 rounded-lg shadow-md">
            <% if (files.length> 0) { %>
                <h2 class="text-xl font-semibold text-gray-800 mb-2">Task List</h2>
                <ul class="list-disc pl-5 space-y-2">
                    <% files.forEach(function(val) { %>
                        <li class="flex items-start">
                            <span
                                class="bg-green-500 text-white rounded-full w-6 h-6 flex items-center justify-center mr-2">✓</span>
                            <p class="text-gray-700">
                                <%= val %>
                            </p>
                            <div class="flex w-full justify-between">
                                <div class="flex w-full justify-between">
                                    <a href="/file/<%= val %>"
                                        class="bg-blue-500 text-white font-bold py-2 px-6 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200 ml-4" >
                                        Read
                                    </a>
                                </div>
                                <a class="bg-blue-500 text-white font-bold py-2 px-6 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200 ml-4"
                                    href="/edit/<%= val %>">Edit</a>
                            </div>
                        </li>
                        <% }); %>
                </ul>
                <% } else { %>
                    <h3 class="text-gray-700">No Task Yet</h3>
                    <% } %>
        </div>
    </div>
    <script src="/javascript/script.js"></script>
</body>

</html>